<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.LightVoice" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Listen and Ring surfaces are opaque black full-screen, not floating: they are
         the only thing on screen and must be readable with the phone at arm's length. -->
    <style name="Theme.LightVoice.Listen" parent="Theme.LightVoice">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
    <style name="Theme.LightVoice.Ring" parent="Theme.LightVoice">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
